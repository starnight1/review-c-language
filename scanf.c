# include <stdio.h>/*从键盘上输入两个数并再次打印出来，比如说1,2*/
void main()
{
	int a,b;
	printf("请输入ab的值:");
	scanf("%d和%d",&a,&b);/*变量值的赋值地址&a和&b之间要加逗号，前面的除了
	%d和%d，其余的在从键盘上输入时要加上，对本例子，输入ab时为：1和2*/
	printf("a=%d\n,b=%d\n",a,b);/*除了%d其余的字符原样打印*/
	
	
	
	
	
/*
	int a=1,b=2,c=321;
	printf("%2d",a);/*长度为2，不足则左边补一个空格*/
/* printf("%-2d\n",b);/*长度为2，不足则右边补一个空格即左对齐*/
/*	printf("%2d",c);/*长度为2，当实际长度大时则按原样长度输出*/
}